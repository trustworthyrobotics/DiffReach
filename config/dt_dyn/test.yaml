state_dim: 2
action_dim: 0
nn_dyn: true
n_total_steps: 20
step_size: 1
discrete: true

sr_window_size: 1000
sim_n_samples: 64

variable_names: [x1, x2]
# dynamics: "models/dynamics/dt_dyn/test.py"
dynamics: "models/dynamics/dt_dyn/test_nn.onnx"

initial_set:
  - [-0.4, -0.3]  # x1
  - [-0.4, -0.3]  # x2
# splits: {0: 4}

output_dir: "output/dt_dyn/test"

visualization:
  vis_stride: 1
  aggregate_partitions: true
  # draw_dims: [0, 1]